using UnityEngine;
using System.Collections;

public class HouseScript : MonoBehaviour {
	/*private Transform t;
	//private GameObject controllSoldier;

	void Start() {
		t = GetComponent<Transform> ();
		//controllSoldier = GameObject.Find ("ControllSoldier");
	}

	void OnMouseDown() {
		print ("click down");
		if (controllSoldier.GetComponent<ControllSoldierScript> ().GetFirstClick ()) {
			controllSoldier.GetComponent<ControllSoldierScript> ().SetFirstClick(false);
			controllSoldier.GetComponent<ControllSoldierScript> ().SetPoint1(t);
			print(t);
		} else {
			controllSoldier.GetComponent<ControllSoldierScript> ().SetFirstClick(true);
			controllSoldier.GetComponent<ControllSoldierScript> ().SetPoint2(t);
			print(t);
		}
		Destroy (gameObject, 1);
	}*/

	/*void OnMouseUp() {
		print ("click up");
	}*/
}
